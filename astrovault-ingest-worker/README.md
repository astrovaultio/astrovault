# AstroVault ASIAIR Ingest Worker

Separate SMB-based ingest worker that connects directly to ASIAIR over network and registers frames through backend internal API.

It is read-only on SMB and never modifies/deletes ASIAIR files.
